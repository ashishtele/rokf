# Run okf command and parse JSON output

Run okf command and parse JSON output

## Usage

``` r
okf_run(args, bundle = "knowledge", verbose = FALSE, parse_json = TRUE)
```

## Arguments

- args:

  Command arguments

- bundle:

  Path to knowledge bundle (default: "knowledge")

- verbose:

  Show command being run

## Value

Parsed JSON or raw output
